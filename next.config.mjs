/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/reactjs-study-app",
  assetPrefix: "/reactjs-study-app/",
  trailingSlash: true,
  agentRules: false,
};

export default nextConfig;
